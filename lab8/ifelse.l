%{
#include "ifelse.tab.h" /* Include the header file generated by Bison */
#include <stdlib.h>
#include <stdio.h>
%}
%%
"if" { return IF; }
"else" { return ELSE; }
[a-zA-Z_][a-zA-Z0-9_]* { return ID; }
[0-9]+ { return NUM; }
"=" { return '='; }
";" { return ';'; }
"{" { return '{'; }
"}" { return '}'; }
[ \t] { /* Ignore whitespace */ }
\n return NL;
. { fprintf(stderr, "Invalid character: %s\n", yytext); }
%%
int yywrap(void) {
return 1;
}